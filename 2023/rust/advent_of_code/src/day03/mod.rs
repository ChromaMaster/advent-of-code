pub fn run_a(input: &Vec<String>) -> String {
    let result = 0;

    result.to_string()
}

pub fn run_b(input: &Vec<String>) -> String {
    let result = 0;

    result.to_string()
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::read_input_file;

    #[test]
    fn is_true() {
        assert!(true);
    }
}
